# Two-Robot Custom Gripper Media

- `ppo/rollout_seed_206.gif`: trained PPO policy success rollout.
- `untrained/rollout_seed_206.gif`: untrained random policy failure rollout with the same seed.
- `stills/`: selected PNG frames for slides.

Success rollout final state: `completed=True`, `collapsed=False`, `floor_dropped=True`, `target_displacement≈0.270m`, `non_target_gripper_contacts=0`.
